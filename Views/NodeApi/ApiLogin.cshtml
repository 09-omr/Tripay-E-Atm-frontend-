@{
    ViewBag.PageTitle = "API Giriþ Test";
}

<div class="row">
    <div class="col-lg-5 mx-auto">
        <div class="text-center mb-4">
            <div class="mb-3">
                <i class="bi bi-cloud-arrow-up text-primary" style="font-size: 4rem;"></i>
            </div>
            <h1 class="display-6 fw-bold">API Giriþ Test</h1>
            <p class="text-muted">Node.js REST API'ye giriþ yapýn</p>
        </div>

        <div class="card shadow border-0">
            <div class="card-body p-4">
                <form asp-action="ApiLogin" method="post">
                    <div class="mb-3">
                        <label class="form-label">E-posta</label>
                        <input name="email" type="email" class="form-control" placeholder="ornek@test.com" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Þifre</label>
                        <input name="password" type="password" class="form-control" placeholder="••••••••" required />
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="bi bi-box-arrow-in-right"></i> API'ye Giriþ Yap
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Geri
                        </a>
                    </div>
                </form>
            </div>
        </div>

        <div class="alert alert-info mt-3">
            <h6><i class="bi bi-info-circle"></i> Test Bilgileri</h6>
            <p class="mb-0 small">
                Eðer Node.js API'de kullanýcý oluþturduysanýz, o bilgilerle giriþ yapabilirsiniz.<br />
                Veya <a asp-action="ApiRegister">buradan</a> yeni kullanýcý oluþturun.
            </p>
        </div>

        <div class="card border-warning mt-3">
            <div class="card-body">
                <h6 class="text-warning"><i class="bi bi-exclamation-triangle"></i> Önemli Not</h6>
                <p class="small mb-0">
                    Bu iþlem Node.js API'ye (<code>http://localhost:3000</code>) baðlanýr.<br />
                    API'nin çalýþtýðýndan emin olun!
                </p>
            </div>
        </div>
    </div>
</div>
